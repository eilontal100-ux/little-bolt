{
  "name": "little-bolt-platformer",
  "version": "1.0.0",
  "private": true,
  "description": "An original five-stage canvas platformer with dash, power-ups and a guardian encounter.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.cjs",
    "test:browser": "node test/browser.cjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
